

// Parasite Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("parasite");
  
  // Get the button that opens the modal
  var btn = document.getElementById("myBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // Parasite Açılma JS Bitiş 

  
  // 47METER Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("meterdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("meterBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // 47METER Açılma JS Bitiş 

  // teenwolf Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("teenwolfdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("teenBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // teenwolf Açılma JS Bitiş 

   // lacasadepapel Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("papelfdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("papelBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // lacasadepapel Açılma JS Bitiş 

   // tomvejerry Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("tomdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("tomBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // tomvejerry Açılma JS Bitiş 

   // dead Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("deaddiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("deadBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //dead Açılma JS Bitiş 

  // lucy Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("lucydiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("lucyBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //lucy Açılma JS Bitiş 

  // elite Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("elitediv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("eliteBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //elite Açılma JS Bitiş 
  // deniz Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("denizdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("denizBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //deniz Açılma JS Bitiş 

  // you Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("youdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("youBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //you Açılma JS Bitiş 

   // pinokyo Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("pinokyodiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("pinokyoBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //pinokyo Açılma JS Bitiş 

   // alice Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("alicediv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("aliceBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //alice Açılma JS Bitiş 

   // green Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("greendiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("greenBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //green Açılma JS Bitiş 

   // allofusaredead Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("alldiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("allBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //allofusaredead Açılma JS Bitiş 

   // wanted Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("wanteddiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("wantedBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //wanted Açılma JS Bitiş 

   // dark Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("darkdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("darkBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //dark Açılma JS Bitiş 

   // bergen Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("bergendiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("bergenBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //bergen Açılma JS Bitiş 

   // breakingbad Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("breakingdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("breakingBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //breakingbad Açılma JS Bitiş 

   // birdbox Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("birddiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("birdBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //birdbox Açılma JS Bitiş 

   // walkingdead Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("walkingdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("walkingBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //walkingdead Açılma JS Bitiş 
  
   // cebimdekiyabanci Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("cebimdekidiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("cebimdekiBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //cebimdekiyabanci Açılma JS Bitiş 

   // wonderwoman Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("wonderdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("wonderBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //wonderwoman Açılma JS Bitiş 

   // platform Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("platformdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("platformBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //platform Açılma JS Bitiş 

   // mezarlik Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("mezarlikdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("mezarlikBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  //mezarlik Açılma JS Bitiş 

  // cizgilidiv Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("cizgilidiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("cizgiliBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // cizgilidiv Açılma JS Bitiş 

   // therainBtn Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("theraindiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("therainBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // therainBtn Açılma JS Bitiş 


   // howBtn Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("howdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("howBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // howBtn Açılma JS Bitiş 



   // legaciesBtn Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("legaciesdiv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("legaciesBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // legaciesBtn Açılma JS Bitiş 


   // mucizeBtn Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("mucizediv");
  
  // Get the button that opens the modal
  var btn = document.getElementById("mucizeBtn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // mucizediv Açılma JS Bitiş 


   // the100Btn Açılma JS Başlangıç

  // Get the modal
  var modal = document.getElementById("the100div");
  
  // Get the button that opens the modal
  var btn = document.getElementById("the100Btn");
  
  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];
  
  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }
  
  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }
  
  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
  // the100Btn Açılma JS Bitiş 

